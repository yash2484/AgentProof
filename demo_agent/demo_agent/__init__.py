"""AgentProof demo research-assistant agent.

A LangGraph multi-agent pipeline (planner -> retriever -> writer -> fact_checker)
instrumented via the agentproof SDK. Runs live (Anthropic) or key-free (replay).
"""

__version__ = "0.1.0"
