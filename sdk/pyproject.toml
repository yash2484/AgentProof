[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
# Distribution name only. The import stays `agentproof` (see tool.hatch below),
# the way `pip install python-dateutil` gives you `import dateutil`.
# Plain `agentproof` on PyPI belongs to an unrelated project in the same niche.
name = "agentproof-sdk"
version = "0.1.0"
description = "Collector SDK for AgentProof — a CI regression gate for teams shipping an agent as a product"
# Must be inside the project directory — hatchling rejects a path outside it,
# and a file above the package root would not ship in the sdist anyway.
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Yash", email = "yashsaksena2484@gmail.com" },
]
dependencies = [
    "pydantic>=2.0",
    "httpx>=0.25.0",
    "typing-extensions>=4.8",
]

[project.optional-dependencies]
langgraph = ["langgraph>=0.2.0"]
autogen = ["autogen-agentchat>=0.4.0"]
all = ["langgraph>=0.2.0", "autogen-agentchat>=0.4.0"]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23"]

[tool.hatch.build.targets.wheel]
packages = ["agentproof"]